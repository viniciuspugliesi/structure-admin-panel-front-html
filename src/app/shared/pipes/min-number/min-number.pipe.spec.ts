import {MinNumberPipe} from './min-number.pipe';

describe('MinNumberPipe', () => {
    it('create an instance', () => {
        const pipe = new MinNumberPipe();
        expect(pipe).toBeTruthy();
    });
});
